node_modules/
.env
.env.local
.env*.local
config/serviceAccountKey.json
auth_info_baileys/
auth/

# Los dumps/backups quedan afuera: son datos reales (DNI, telefonos) que no van al repo.
*.sql
# Solo los stored procedures se versionan.
!migrations/sp/*.sql
